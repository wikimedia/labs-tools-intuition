language: php
php:
  - "5.4"
  - "5.3"
install:
  - composer install
  - npm install -g grunt-cli
  - npm install
script:
  - composer test
  - npm test
notifications:
  irc:
    channels:
      - "chat.freenode.net##krinkle"
